apiVersion: v1
kind: Service

metadata:
  name: cats-dogs-api

spec:

  type: NodePort

  selector:
    app: cats-dogs-api

  ports:

    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080